# CI Workflow File - needs to be at .github/workflows/ci.yml

This is a placeholder. The actual CI workflow file needs to be created at
`.github/workflows/ci.yml` via the GitHub web UI because the API token
lacks the `workflow` scope.

See issue #6 and issue #15 for details.
